export const mapConfig = {
    minZoom: 6,
    maxZoom: 20, // Incrementa il massimo livello di zoom
};

export const initialZoom = 8;
export const initialCoordinates = { lat: 41.10871, lng: 16.87884 };
